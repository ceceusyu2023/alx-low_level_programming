#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _strdup - duplicate to new memory space location
 * @str: char
 *
 * Return: 0
 */
char *_strdup(char *str)
{
	char *a;
	int i, r = 0;

	if (i == NULL)
		return (NULL);
	for (r = 0; str[r]; r++)
		a[r] = str[r];
	return (a);
}
